# Native workspace

`drakes-graph` prepara calculos de redes y energia; `drakes-storage` prepara el limite para RocksDB. Ambos mantienen ABI pendiente y no se invocan desde eventos calientes de Paper.
